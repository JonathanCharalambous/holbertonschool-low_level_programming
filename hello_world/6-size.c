#include <stdio.h>
/**
 * main - Prints the size of various primitive data types found in C
 * Return: Always 0 (Success)
*/
int main(void)
{
	sizeof(char);
	sizeof(int);
	sizeof(long);
	sizeof(double);
	sizeof(float);
	return (0);
}
