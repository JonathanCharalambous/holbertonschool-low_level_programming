#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * mul - multiples two variables
 * @a: The first number to multiply
 * @b: The first number to multiply
 * Return: multiple of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
